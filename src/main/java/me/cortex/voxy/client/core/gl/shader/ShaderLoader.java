package me.cortex.voxy.client.core.gl.shader;

/**
 * 存根类，仅在编译时使用，运行时将被 Voxy 的实际类替代。
 */
public class ShaderLoader {
    public static String parse(String id) {
        // 此方法不会被调用，仅用于让编译器通过
        return "";
    }
}
